/**
 * Company identity used to build job IDs (`{company}-{branch}-{sequence}`).
 * `AP` is the Alpine Peaks placeholder from the design — swap it for the real
 * company's initials.
 */
export const COMPANY_INITIALS = 'AP';

/** How long a toast stays on screen, in milliseconds. */
export const TOAST_DURATION_MS = 2600;
